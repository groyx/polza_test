# --- сеть ---
# curl_cffi подменяет TLS-отпечаток. Без него hh.ru и часть корпоративных
# сайтов за Cloudflare отдают 403 даже с правильным User-Agent.
curl_cffi>=0.7
requests>=2.31

# --- разбор HTML ---
beautifulsoup4>=4.12
lxml>=5.0

# --- проверка почты ---
# MX-записи. В стандартной библиотеке резолвера DNS нет.
dnspython>=2.6

# --- таблицы ---
pandas>=2.0
openpyxl>=3.1

# --- фаззи-матчинг имён компаний с доменами (аудит базы) ---
rapidfuzz>=3.6

# --- LLM ---
# Опционально: нужен только для бэкенда `anthropic`.
# Бэкенд `openai_compat` работает без него.
anthropic>=0.40
